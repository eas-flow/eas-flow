# skills/

Claude Code skills provided by the eas-flow plugin. Each skill is a directory
containing a `SKILL.md`.

Planned (v0.1.0): `deploy`, `release-draft`.
Planned (v0.2.0): `develop-work`, `doc-sync`.
